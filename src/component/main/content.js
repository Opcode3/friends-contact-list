import React, {useState} from "react";
import './content.css'

// const [first, setfirst] = useState()

export default function Content({title}){
    return (
        <main>
            <h4>{title}</h4>
        </main>
        );
}